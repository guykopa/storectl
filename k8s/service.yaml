apiVersion: v1
kind: Service
metadata:
  name: storectl
  namespace: storectl
  labels:
    app: storectl
spec:
  selector:
    app: storectl
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
